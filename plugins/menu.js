mndMessage(msg.key.remoteJid, {
            text: "RED ZONE MD MENU\n. ping\n. menu"
        })
    }
}odule.exports = {
    pattern: "menu",
    run: async (sock, msg, args) => {
        await sock.se
